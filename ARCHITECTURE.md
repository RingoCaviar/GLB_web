# GLB Web 项目地图

> 后续修改先读本文件；完成后同步更新受影响条目与“最近维护”。只记录稳定事实，避免复制代码细节。

## 1. 项目目标

- 用 Google `<model-viewer>` 在浏览器中选择并查看 `models/` 下的多个 GLB 模型。
- 支持轨道相机、FOV、背景和指定分辨率 PNG 导出。
- 通过 Vite 在本机及局域网提供静态网页，不含独立后端、数据库或上传功能。
- Vite 内部提供仅服务主机可用的缩略图与默认光照落盘能力，局域网访问者保持只读。

## 2. 技术与运行

- 运行时：Node.js + npm。
- 前端：原生 HTML、CSS、JavaScript。
- 核心依赖：`@google/model-viewer@4.3.1`。
- 构建工具：`vite@7.0.6`。
- 开发端口：5173；`npm run dev` 监听 `0.0.0.0`。
- 构建：`npm run build`，产物在 `dist/`。
- 中文服务管理：双击根目录 `启动服务.bat`；脚本必须保持 ANSI CP936 + CRLF。

## 3. 目录地图

| 路径 | 职责 | 修改提示 |
| --- | --- | --- |
| `index.html` | 页面入口、元信息、挂载 `src/main.js` | 仅处理文档级配置，不放业务逻辑 |
| `src/main.js` | UI 模板、页面状态、相机同步、导出和事件处理 | 功能行为的首要定位点 |
| `src/model-loading.js` | 模型加载会话的选择、取消、终态分类与状态快照 | 页面入口仅通过 `select()` 和 `getSnapshot()` 使用；不要把查看器时序或 DOM 规则泄漏进此 module |
| `src/model-viewer-loading-adapter.js` | 将 model-viewer 的加载事件和 `src` 操作转换为模型加载会话可使用的查看器 adapter | 浏览器实际 adapter 与测试 fake 共同定义该 seam |
| `src/model-library.js` | 用 Vite 静态资源清单递归发现 GLB、封面并生成目录导航数据 | 模型目录、排序或封面规则从此处修改 |
| `src/thumbnail-generator.js` | 用独立隐藏查看器生成固定默认视角的 640×480 WebP | 不得复用或改变主查看器状态 |
| `src/fixed-size-render.js` | 统一固定尺寸出图的临时状态、稳定等待、截图、尺寸验证与失败恢复 | PNG 导出和自动缩略图必须通过此 module，格式合成仍留在各自 adapter |
| `src/camera-constraints.js` | 轨道方向、包围盒表面半径和 FOV 可达性纯计算 | 不依赖 DOM/WebGL，可由盲测直接导入 |
| `src/camera-zoom.js` | 滚轮归一化、距离缩放和双指间距纯计算 | 不依赖 DOM/WebGL，可由盲测直接导入 |
| `src/shared-camera-framing.js` | 读写跨模型、跨刷新的共享相机角度、FOV 与垂直镜头偏移 | 距离始终由当前模型自动取景 |
| `src/camera-framing-application.js` | 统一应用共享相机取景 | 相机更新后的投影钩子是移轴重施的唯一高层 seam |
| `src/vertical-lens-shift.js` | `<model-viewer>` 垂直镜头偏移兼容层 | 隔离固定版内部符号，业务代码不得直接访问内部场景相机 |
| `src/render-quality.js` | 为固定版 model-viewer 应用 GPU 最大各向异性与最佳纹理过滤 | 兼容层失败时回退公开 Sampler API，不得阻断模型加载 |
| `src/model-appearance.js` | 决议模型级光照、材质覆盖与项目默认光照，并管理模型外观的浏览器存储 | 交互预览和自动缩略图必须通过此 module 决议；损坏光照回退项目默认且保留可读材质覆盖 |
| `src/render-studio.js` | HDRI、光照、材质编辑及已决议模型外观的应用 | 不得重写模型外观的优先级或存储规则；只作为参数定义 module 的浏览器 adapter |
| `src/render-studio-parameters.js` | 渲染工作室可编辑参数的范围、格式化、基准比较与分组计数 | 参数 UI、单参数重置与无 DOM 测试必须从此 module 读取规则 |
| `src/coalesced-writer.js` | 将连续模型外观快照按空闲窗口合并为一次写入 | 不依赖 DOM；计时器可替换以便盲测 flush 与 cancel |
| `scripts/environment-manifest-plugin.mjs` | 自动发现环境目录内 HDR/EXR、生成 EXR 兼容缓存、全景缩略图与供前端导入的环境清单 | 缓存产物只写入 `public/environments/.generated/`，不得改动源环境文件；受控的 `src/environment-presets.generated.js` 是唯一清单源码例外 |
| `src/lighting-defaults.js` | 读取和保存项目级默认光照的前端请求 | 保存失败不得影响当前预览和模型级设置 |
| `tests/camera-constraints.mjs` | 相机极端参数盲测 | 运行 `npm run test:camera` |
| `tests/model-appearance.mjs` | 模型外观优先级、损坏回退与重置重新决议 | 运行 `npm run test:model-appearance` |
| `src/style.css` | 桌面侧栏、模型舞台、移动端适配和控件样式 | 820px、480px 为现有响应式断点 |
| `models/**/*.glb` | 模型库自动发现的模型资源 | 支持中文、空格和多级目录，由 Vite `import.meta.glob` 生成安全 URL |
| `models/注射器包装盒/*.png` | 4096×4096 外置源贴图备份 | 当前运行时不直接加载 |
| `启动服务.bat` | 项目内运行时/依赖安装、检查、启动、状态、停止与分级安全清理 | 编辑后必须重新转为 ANSI CP936 + CRLF；安装和清理不得影响项目外路径 |
| `scripts/start-vite.mjs` | 分离启动实际 Vite Node 进程并将输出接入日志 | 只向批处理返回服务 PID，避免后台句柄阻塞 |
| `scripts/manage-local-runtime.ps1` | 下载并校验官方 Node.js 22 LTS ZIP，安装或删除项目专属运行时 | 所有写入与删除必须限制在根目录 `.runtime/`，不得修改系统 PATH |
| `scripts/model-metadata-plugin.mjs` | 在 Vite 启动/构建时读取 GLB 文件加入时间并生成虚拟元数据模块 | 最近模型排序以该构建期快照为准；资源变化后需重启或重建 |
| `scripts/thumbnail-middleware.mjs` | 校验本机请求、模型路径和 WebP 后原子写入规范封面 | 写入目标必须限制在真实 `models/` 根目录内 |
| `scripts/lighting-default-middleware.mjs` | 提供项目默认光照读取及仅主机可写接口 | 只允许写入固定的 `config/default-lighting.json` |
| `scripts/sequence-cache.mjs` | 动态扫描 360° 产品、维护缓存签名并调度并行生成 | 产品 ID 和资源路径必须限制在 `image_sequence/` |
| `scripts/sequence-middleware.mjs` | 提供动态产品清单、受限资源和本机构建任务接口 | 局域网请求不得创建任务 |
| `vite.config.js` | 注册缩略图中间件并忽略生成封面的 HMR 事件 | 开发与 preview 都需保留本地写入能力 |
| `package.json` | 固定依赖版本和 dev/build/preview 命令 | 变更依赖后同步 lockfile |
| `README.md` | 面向使用者的安装、局域网访问和导出说明 | 操作方式改变时同步更新 |
| `.agent/skills/glb-web-project/` | 本项目专属 Codex 工作流 | 不放产品运行代码 |
| `.gitignore` | 排除依赖、构建产物、服务状态、日志和编辑器文件 | 新增临时产物时同步维护 |

## 4. 页面结构与状态

```text
#app
└─ .workspace
   ├─ .stage
   │  ├─ 标题与模型状态
   │  └─ .viewer-shell
   │     ├─ .model-library（目录导航与缩略图卡片覆盖层）
   │     ├─ .viewer-frame（与导出比例一致）
   │     │  ├─ <model-viewer>
   │     │  └─ 取景边框与分辨率标签
   │     ├─ 加载/错误状态
   │     └─ 操作提示
   └─ .panel
      ├─ 相机：theta / phi / radius / fov
      ├─ 画面：background
      └─ 导出：width / height / exportQuality / transparent
```

`src/main.js` 中的 `state` 是页面状态中心：

- 模型库：模型描述列表、当前文件夹、当前模型、模型库开关、缩略图写入能力和生成任务；加载令牌由模型加载 module 私有持有。
- 相机：水平角 `theta`、俯仰角 `phi`、米制实际距离 `radius`、垂直视场角 `fov`、按画面高度归一化的垂直镜头偏移 `verticalShift`。
- 导出：`width`、`height`、导出质量档位、贴图锐度、透明背景和背景颜色。
- 生命周期：模型是否加载完成、是否正在导出。
- 渲染质量：最大各向异性是否应用、兼容层状态和实际渲染比例。
- 自定义加载进度层只在加载过程中显示，`load` 或 `error` 终态必须立即隐藏。

## 5. 关键数据流

### 相机

1. 控件输入调用 `setControl()` 更新状态和两个输入组件。
2. `applyCamera()` 通过统一相机应用入口写入 `<model-viewer>` 的 `cameraOrbit` 与 `fieldOfView`，随后重施垂直镜头偏移。
3. 模型加载后用公开 AABB、实时相机目标和轨道射线计算进入/离开区间；目标在盒内时以离开表面为最小半径，目标在盒外时把射线穿盒区间视为禁止距离。
4. 滚轮、触控板、双指和缩放按键由捕获层按动画帧合并，只改变实时轨道距离；连续移动按原来所在侧钳制到近侧或远侧安全表面，禁止一次大步长穿过模型。
5. 旋转、平移或目标变化后重新检查实际相机位置；若已进入 AABB，立即推到位移最小的安全表面并同步轨道、距离控件与组件约束。
6. 只有来源为 `user-interaction` 的 `camera-change` 才按实时值回写角度与距离；FOV 由面板状态单向持有，直接交互产生的意外偏移会被恢复。
7. FOV 面板改变时以前一次稳定目标距离和目标 FOV 为基准补偿距离；若补偿距离触碰表面或禁止区间，则固定安全距离并反算可保持构图的有效 FOV。
8. 距离数值没有固定最大值；超过当前滑杆或轨道容量时，先等待轨道上限扩展生效，再应用相机目标，且容量只增不减。
9. 数值输入框聚焦期间暂停对应相机参数的插值回写，避免用户正在编辑的内容被中间状态覆盖。
10. “建筑校正”将俯仰角一次性设为 90°，但不锁定后续交互；竖线平行依赖相机保持水平，垂直镜头偏移只移动偏心透视取景窗口。
11. 垂直镜头偏移范围为画面高度的 -50%～+50%，属于共享相机取景；偏移为 0 时清除 view offset，恢复组件原生投影。
12. 固定版 `@google/model-viewer@4.3.1` 没有公开的垂直偏移 API。唯一兼容层使用内部场景符号取得 Three.js 透视相机并调用 `setViewOffset()`；能力探测失败时禁用移轴，不影响普通相机。AR/XR 不应用此兼容层。

### 模型库与切换

1. `model-library.js` 在 Vite 启动/构建时递归扫描 GLB；文件夹保持层级，当前目录只显示直属模型和下级目录。Vite 虚拟元数据模块同步提供文件加入时间，模型库首页按时间倒序展示最多 10 个“最近更新”模型并允许直接打开。
2. 封面按“同名 `.cover.*`、目录 `cover.*`、内置占位图”回退，不加载 GLB 自动截图，也不匹配普通材质贴图。
3. 页面首次打开不设置查看器 `src`；相机和导出保持禁用，直到用户选择模型且收到有效 `load` 终态。
4. 切换开始时递增加载令牌并清空上一模型的包围盒、相机容量、碰撞边界、纹理注册和异步同步任务；迟到的异步结果必须核对令牌后才能写回。
5. 新模型成功后重新自动取景、建立 AABB 约束并应用纹理质量；随后恢复跨模型、跨刷新的共享水平角、俯仰角、FOV 与垂直镜头偏移，距离始终使用新模型的自动取景值。相机重置会覆盖该共享取景，画面与导出设置保持不变。
6. 加载失败时保持控制和导出禁用，显示具体模型名并重新打开模型库。PNG 文件名取当前模型安全化名称。
7. 桌面端右键模型卡片可启动独立隐藏查看器，以当前共享相机取景和浅灰背景生成 640×480 WebP；距离由缩略图模型自动取景，任务不写入主查看器状态，同一时间只运行一个。右键菜单挂载在 `body` 顶层，使用视口坐标和菜单实际尺寸定位，避免模型库的滤镜、裁剪与滚动容器造成视觉偏移或点击命中错误。
8. 保存接口仅接受本机网卡来源且 Origin 与 Host 一致的 JSON 请求；模型 ID、真实路径、文件类型、WebP 头和尺寸均通过后，才写入 `<模型名>.cover.webp`。
9. 生成封面的文件事件不触发 Vite 整页热更新；前端用保存响应的版本化 URL局部刷新卡片。局域网设备可读取封面但不可调用写入。

### 渲染工作室与默认光照

1. HDRI 预设同时提供原色和保留亮度的灰度资源；`desaturate` 和背景预览默认开启，预览通过公开 `skyboxImage` 显示当前环境全景。
2. Vite 启动时自动发现 `public/environments/` 根目录的 `.hdr` 与 `.exr`；`.grayscale.hdr` 作为同名环境的去色资源而非独立选项。EXR 自动转换为 model-viewer 可用的缓存 HDR，所有源文件自动生成全景 WebP，缓存只写入被忽略的 `.generated/`。
3. HDRI 通过显示环境图本身的等距柱状全景缩略图卡片切换；中性光以无 HDRI 占位图呈现。选择器支持键盘导航，加载失败时保留上一个成功环境并标记失败卡片。
4. HDRI 环境旋转按俯仰 X（-180°～180°）、水平 Y（0°～360°）和翻滚 Z（-180°～180°）进入光照状态。固定版本兼容层把三轴同步到环境与普通背景，带地面的天空盒仅跟随 Y，以保持地平线水平；旧 `environmentRotation` 状态兼容为 Y 轴。
5. 页面启动由模型外观 module 读取 `config/default-lighting.json`。模型没有当前浏览器的模型级外观记录时使用项目默认；已有模型级记录时继续优先恢复该记录；损坏光照回退项目默认但保留可读材质覆盖。
6. “保存为默认”把当前光照原子写入固定配置文件。接口只允许服务主机且要求 Origin 与 Host 一致；局域网访问者可读取但按钮不可用。
7. 连续数值与颜色参数提供始终占位的单参数重置；已处于基准值时禁用。光照回项目默认、材质回 GLB 原始值、相机回自动取景值，背景色与导出尺寸回项目初始值；整组重置继续保留。
8. “重置光照”和“恢复模型默认效果”恢复当前项目默认值，不再硬编码回内置参数；连续参数均以滑杆和数值输入共同编辑，输入失焦或提交时钳制到参数定义的范围。
9. 参数变更即时应用到预览，模型外观浏览器存储以 200ms 空闲窗口合并；基础光照、三轴环境旋转和材质参数分别显示相对基准的已调整数量，三轴环境旋转与高级材质参数默认折叠且不持久化。
10. HDRI 背景只用于交互预览；PNG 导出期间临时关闭 skybox，透明/背景色语义保持不变，自动封面也不显示 HDRI 背景。

### PNG 导出

固定尺寸出图由 `fixed-size-render.js` 统一执行。主查看器 PNG 导出与隐藏查看器自动缩略图共享同一个 seam：module 接管尺寸、位置、HDRI 背景、阴影和纹理采样状态，等待渲染稳定并验证实际像素尺寸，随后无论成功、失败或取消都按相反顺序恢复。PNG 背景合成与 WebP 编码仍由各自 adapter 完成。

1. 校验并限制宽高为 64 像素至当前设备报告的单边理论上限。
2. 临时按目标尺寸与设备 DPR 调整 `<model-viewer>`，保持其位于视口以持续渲染。
3. 调用官方 `toBlob()` 获取透明原始渲染；非透明导出通过 Canvas 先填充所选背景色再合成模型。高质量档在最终尺寸 Canvas 上应用颜色保持的 FXAA 边缘平滑，透明导出保留透明通道。
4. 下载包含模型名、分辨率和时间戳的 PNG，最后恢复查看器样式。
5. `minimumRenderScale = 1` 不可随意删除，否则高分辨率可能被动态降采样。
6. 截图前读取实际 WebGL 绘制缓冲宽高；小于请求尺寸时以可诊断错误中止，禁止生成尺寸正确但内容裁剪的 PNG，并将实际支持的宽高回填到导出输入框。
7. 导出质量与贴图锐度独立：标准默认自适应 mipmap，高质量默认原图优先；两者均使用最大各向异性过滤。原图优先临时锁定纹理基础层以保持斜面文字和细线，高质量另在最终 PNG 的高对比边缘应用 FXAA，避免提高固定尺寸出图的画布大小。
8. 透明导出默认临时关闭地面阴影；启用“保留阴影”时保留半透明投影。阴影强度在导出结束或失败后恢复，非透明导出始终沿用当前摄影棚阴影。
9. 固定尺寸出图在目标尺寸生效后按该尺寸重施归一化垂直镜头偏移；事务结束恢复原尺寸后再次重施预览投影，成功、失败和取消路径使用相同恢复规则。

### 渲染质量与取景框

1. 模型加载后，`render-quality.js` 通过固定版 `@google/model-viewer@4.3.1` 的场景/渲染器符号遍历 Three.js 模型纹理，应用 GPU 支持的最大各向异性、三线性 mipmap 和线性放大过滤。
2. 兼容访问不可用时，仅使用公开材质 Sampler 设置最佳通用过滤并给出提示；不得修改 `node_modules`、GLB 或源贴图。
3. `minimumRenderScale = 1` 配合原生 DPR 保持预览采样；`render-scale` 低于 1 时在消息区提示设备降采样，导出仍尝试完整目标像素。
4. 导出宽高是取景框的唯一比例来源。实际 `<model-viewer>` 画布居中内接于 `.viewer-shell`，框外由遮罩压暗，因此框内投影与最终 PNG 一致。
5. 宽高有效输入实时刷新取景框，失焦时钳制到 64 像素至设备报告的理论上限；`ResizeObserver` 在窗口和响应式布局变化时重新计算。
6. 导出时隐藏边框、标签和操作提示，等待查看器尺寸、纹理和相机稳定后截图；高质量档仅在最终图像应用 FXAA。
7. 预览始终使用三线性 mipmap；导出质量档位作为浏览器全局偏好保存，首次默认为标准，状态区报告 FXAA、贴图锐度和 GPU 最大各向异性。

### 服务脚本

1. 菜单可将 Node.js 22 LTS、npm 缓存与 npm 依赖全部安装在当前项目；安装包依据官方 `SHASUMS256.txt` 校验，且不修改系统 PATH。运行时解析始终优先 `.runtime/node/`，不存在时才回退系统 Node/npm。
2. 完整检查可验证 Node、npm、模型及 npm 依赖；启动路径只做必要文件快速检查，避免 `npm list` 拖慢启动。
3. 批处理使用解析后的项目内或系统 Node 调用 `scripts/start-vite.mjs`，分离启动项目内 `node_modules/vite/bin/vite.js`，避免 npm/PowerShell 包装进程及输出句柄阻塞；保存的 PID 即实际 Vite 服务进程。
4. 启动时同时检查进程存活与 5173 端口监听，成功后立即结束等待并显示地址；输出与错误继续写入独立日志。
5. 状态和停止操作会核对 PID 文件与端口监听进程，区分本项目服务和其他程序占用；停止非脚本记录的进程前必须确认。
6. 删除环境经 `REMOVE` 二次确认后，只删除项目内 `.runtime/`、`node_modules/`、`dist/`、`.vite/` 及服务状态文件；轻量清理经 `CLEAN` 确认且保留运行时和依赖。运行中的本项目服务须先停止，端口属于其他进程时取消；`models/`、源码、配置和 Git 数据始终不在删除范围。

## 6. 约束与验证

- 不引入后端；模型和图片只在访问者浏览器中处理。
- 缩略图写入是 Vite 内部的本机维护接口，不得扩展为局域网通用写入、上传或任意路径文件接口。
- 保持 GLB 的 Vite URL 导入/静态清单，不手写未经编码的公开 URL；新增资源需重启 Vite 或重新构建后才会进入清单。
- 相机 FOV 范围 10°–90°，俯仰角 1°–179°；距离使用 GLB 米制场景单位，近端保留组件安全限制，最大值按需动态扩展。
- 相机/FOV 改动至少运行 `npm run test:camera`，并验证直接缩放不改变 FOV、参数同步、表面边界和恒定构图补偿。
- 垂直镜头偏移改动还需运行 `npm run test:vertical-shift`、`npm run test:vertical-shift-projection`、`npm run test:vertical-shift-browser`、`npm run test:camera-application` 和 `npm run test:shared-camera`。升级 model-viewer 或 Three.js 前必须先通过这些兼容契约、真实浏览器像素门禁与投影回归，内部 seam 变化不得静默忽略。
- 导出改动至少验证 512×512 精确尺寸、透明/不透明背景和失败恢复。
- 通用代码修改后运行 `npm run build`。
- 光照默认值或其落盘接口改动至少运行 `npm run test:studio` 和 `npm run test:lighting`。
- 批处理修改后检查 CP936、无 UTF-8 BOM、CRLF，并至少测试菜单启动退出。

## 7. Git 状态

- 当前工程是 Git 仓库，默认分支为 `main`。
- `node_modules/`、`dist/`、服务 PID 与日志均不纳入版本控制。
- 按项目 Skill 的规则评估提交；提交信息使用中文，并只暂存当前任务文件。

## 8. 最近维护

- 2026-09-05：共享相机取景增加垂直镜头偏移与建筑校正；交互预览、固定尺寸出图和自动缩略图共享归一化偏移，并以兼容契约、投影计算和真实浏览器像素测试约束依赖升级。
- 2026-09-02：参数定义集中到独立 module；渲染工作室由该 module 驱动连续参数的范围、格式化、重置与已调整计数，预览即时更新且模型外观持久化合并。
- 2026-09-02：模型外观决议集中到独立 module；交互预览和自动缩略图共享项目默认、模型级外观与内置默认的回退规则。
- 2026-09-02：模型加载改为私有会话状态与查看器 adapter，切换模型会中止上一加载和固定尺寸出图；HDRI 环境支持自动清单、全景预览与三轴旋转。
- 2026-09-01：HDRI 改为环境全景缩略图卡片选择器并支持三轴环境旋转；连续数值与颜色参数增加独立重置，旧水平旋转状态保持兼容。
