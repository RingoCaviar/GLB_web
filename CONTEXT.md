# GLB 模型取景

用于在浏览器中预览 GLB 模型、调整模型外观，并生成指定尺寸的产品图像。

## Language

**固定尺寸出图**:
按目标像素尺寸与当前模型外观生成图像的过程；PNG 导出和自动缩略图都以它的渲染结果为来源。
_Avoid_: 截屏、截图事务、固定尺寸渲染事务

**模型加载**:
从选择 GLB 模型到该模型可交互，或进入可诊断失败状态的完整会话；会话会取消上一模型尚未完成的固定尺寸出图。
_Avoid_: 打开模型、切换文件、载入流程

**HDRI 环境**:
用于模型照明和交互预览背景的高动态范围环境；中性光是环境选择器中的无 HDRI 选项。
_Avoid_: HDRI 贴图、天空盒

**三轴环境旋转**:
分别控制 HDRI 环境水平、俯仰和翻滚方向的旋转；带地面的天空盒仅跟随水平旋转，以保持地平线水平。
_Avoid_: HDRI 角度、水平旋转

**单参数重置**:
仅将一个可调参数恢复到其所属对象的基准值：光照回到项目默认值、材质回到 GLB 原始值、相机回到自动取景值，背景色和导出尺寸回到项目初始值。
_Avoid_: 归零、恢复出厂设置

**导出质量档位**:
固定尺寸出图采用的抗锯齿级别；标准档以自适应纹理采样为主，高质量档在最终尺寸上应用 FXAA 边缘平滑。
_Avoid_: 画质、抗锯齿开关

**贴图锐度**:
固定尺寸出图采用的纹理层级策略；“自适应”使用 mipmap 保持稳定，“原图优先”临时锁定基础纹理层，以保留斜面文字和细线。
_Avoid_: 贴图质量、纹理清晰度

**模型外观**:
某个 GLB 模型的模型级光照与材质覆盖；两者独立决议，已保存光照优先于项目默认，缺失或不可用时回退到项目默认或内置默认。
_Avoid_: 模型样式、渲染设置

**共享相机取景**:
在模型加载会话之间沿用、并跨页面刷新保留的同一套相机取景状态，不按模型分别保存。
_Avoid_: 每模型相机角度、模型专属视角

**垂直镜头偏移**:
在不改变相机俯仰方向的前提下，沿画面竖直方向移动透视投影窗口，调整对象的画面位置；仅包含竖直方向，不包含水平镜头偏移。
_Avoid_: 移轴角度、相机上移

**建筑透视校正**:
在不改变相机姿态、目标、距离或 FOV 的前提下，将世界竖直方向的消失点映射到无穷远，使渲染中的竖直线彼此平行；与垂直镜头偏移可同时使用。
_Avoid_: 相机水平、抬平相机、改变相对角度
